---
import { getLangFromUrl } from "@i18n/utils";

const props = Astro.props;
const { lang } = Astro.params;
console.log("🚀 ~ lang:", lang)
const _lang = getLangFromUrl(Astro.url);
// pour ne pas indexer deux fois la page index qui avec et sans slug /index et /fr/index
const indexOrNot = lang !== undefined && {"data-pagefind-body":true};
---

<div class="markdown-content" {...indexOrNot}>
  <slot />
</div>
